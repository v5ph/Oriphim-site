# Cloudflare Pages - Redirects and rewrites

# Clean URLs handled by Next.js App Router
# /legal -> app/legal/page.tsx
# /privacy -> app/privacy/page.tsx
# /sla -> app/sla/page.tsx

# Fallback for any 404s to home page (optional, comment out if not desired)
# /* /index.html 404
